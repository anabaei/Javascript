## CRAFT

#### Permission
* storage and config both should have writeable permissions


#### Composer

* Composer is `dependency (package) manager` like `npm` that handle all require libraries to run an app
* It is standard way of working with craft
* Since v3, default installation using composer


#### Account
* Go to User account, check box all boxes to see routes, memory use in each page and time loading
* 